import {createTheme} from '@mui/material/styles';

export const muiThemeProvider = createTheme({
    palette: {
        primary: {
            main: "#17860f",
        },
        secondary: {
            main: "#7e0f86",
        },
    },
});